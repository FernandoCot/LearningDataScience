import numpy as np

my_list = [[1,2,3],[4,5,6],[7,8,9]]

arr = np.array(my_list) # Transforming the list into an array

x = np.arange(0,485, 5) # Generating a new array populated

y = np.linspace(0, 50, 10) # Generating a new array with evenly spaces and decimals
